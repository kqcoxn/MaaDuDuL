## **独立任务**

独立任务指无法与其他任务自动串联执行的任务类型。这类任务通常具有以下特点：

1. **界面独立**：开始界面或结束界面不是主界面，无法与其他日常任务自动衔接
2. **需手动触发**：通常需要用户手动选择执行，不建议加入自动化任务链中
3. **流程特殊**：任务执行流程较为特殊，需要特定的前置条件或后续处理

#### **使用建议**

配置独立任务时，请注意：

- 建议您右键任务标题，使用独立执行任务功能启动
- 不要将独立任务放在可连续任务链中
